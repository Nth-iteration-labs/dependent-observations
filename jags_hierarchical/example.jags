model {

	# Model for the observations (likelihood)
	for (i in 1:N) {
		x[i] ~ dnorm(mu, tau)
	}
	
	# Priors for mu and tau
	# Note random and deterministic difference ( ~ and <- )
	mu ~ dnorm(0, .0001)
	tau <- pow(sigma, -2)
	sigma ~ dunif(0, 100)
}